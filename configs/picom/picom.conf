#################################
#           Backend             #
#################################
# GLX backend is usually faster and prevents tearing better than xrender
backend = "glx";

# VSync is the main reason we are here.
vsync = true;

#################################
#           Shadows             #
#################################
# Disabled for that flat, sharp "Cyber" look
shadow = false;

#################################
#           Fading              #
#################################
# Disabled for instant window snapping
fading = false;

###############################
#           Blur              #
###############################
# Disabled for performance
blur = false;

#################################
#   Transparency / Opacity      #
#################################
# Keep everything opaque for maximum readability (High Contrast)
active-opacity = 1.0;
inactive-opacity = 1.0;

# If you ever change your mind and want a slight see-through terminal:
# opacity-rule = [
#   "95:class_g = 'Kitty'"
# ];

#################################
#       General Settings        #
#################################
# Daemon settings to keep it lightweight
daemon = true;
use-damage = true; 

# Stop picom from messing with your fancy lock screen
# (prevents double-blurring if betterlockscreen does its own thing)
wintypes:
{
  dock = { shadow = false; clip-shadow-above = true; }
  dnd = { shadow = false; }
  popup_menu = { opacity = 1.0; }
  dropdown_menu = { opacity = 1.0; }
};
